/**
 * @fileoverview File that provides the "UserContext" (an user object) to the "ModalContainer.jsx".
 */

import { createContext } from "react";

const UserContext = createContext();

export default UserContext;
