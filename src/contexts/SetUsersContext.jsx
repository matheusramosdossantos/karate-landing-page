/**
 * @fileoverview File that provides the "SetUsersContext" to the "ModalContainer.jsx".
It contains:
setNewUser,
setAllUsers,
setAge,
setEmail,
setName,
setFormButton,
formButton,
setDisableInput,
disableInput.
 */

import { createContext } from "react";

const SetUsersContext = createContext();

export default SetUsersContext;
